export namespace Reservation {
  export namespace API {
    export interface ServerModel {
      titile: string;
      linkIdText: string;
      linkIdUrl:  string;
      period: string;
      example: string;
      note: string;
      reservationNote: string;
    }
  }
}
